server {
  listen 1234;
  listen 1234;

# test if this location with index and get methods are in conflict
  location / {
	root	www;
    allow GET;
    autoindex on;
  }

  location /hello {
	root	www/uploads;
    allow GET;
    allow POST;
    autoindex on;
  }
}
